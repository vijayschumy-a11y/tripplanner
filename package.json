{
  "name": "tripplanner",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack domestic trip planner — expense split, live location, nearby places",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "npm install --prefix server && npm install --prefix client && npm run build --prefix client",
    "start": "node server/src/index.js"
  }
}
